# Ignore files for push...
# Excluded files over than 100MB
# Large files in a GitHub free account has to be compress in 95MB files parts

# General exclusions
.idea/
.git/
.venv/
.venvPython310/
.venvPython311/
**/*.nc
**/*.csv
**/*.grib
__pycache__/
.ppoi/1/awb/shpout/basindissolve/merge/*.shp
.ppoi/1/awb/shpout/basindissolve/merge/*.shx
.ppoi/1/awb/shpout/basindissolve/merge/*.dbf
.ppoi/1/awb/shpout/basindissolve/merge/*.prj
.ppoi/1/awb/shpout/basindissolve/merge/*.cpg
.ppoi/1/awb/shpout/basindissolve/merge/*.xml
.ppoi/1/awb/shpout/watershed/merge/*.shp
.ppoi/1/awb/shpout/watershed/merge/*.shx
.ppoi/1/awb/shpout/watershed/merge/*.dbf
.ppoi/1/awb/shpout/watershed/merge/*.prj
.ppoi/1/awb/shpout/watershed/merge/*.cpg
.ppoi/1/awb/shpout/watershed/merge/*.xml


# Some samples
# http://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository#Ignoring-Files

# ignore all .a files
# *.a

# but do track lib.a, even though you're ignoring .a files above
# !lib.a

# ignore all files in any directory named build
# build/

# ignore doc/notes.txt, but not doc/server/arch.txt
# doc/*.txt

# ignore all .pdf files in the doc/ directory and any of its subdirectories
# doc/**/*.pdf